"use Strict" // treat all js code as newer version
// alert("Hello"); //we are using nodejs not browser

/*
number = 2 ^ 53
bigint = for large numbers
string = "assembly of characters"
boolean = true or false
null = stand alone value (type = object)
undefined = kindda place holder for a value which is not defined yet (type = undefined)
symbol = uniquness
*/

let a=2002; // number
let big_a = 98459437;
let name="Amit"; //string
let study=true; //bool
let state;
let blank=null;

console.table([typeof(a), typeof(big_a),typeof(name), typeof(study),typeof(state),typeof(blank)]);

